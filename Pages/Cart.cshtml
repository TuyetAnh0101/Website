@page
@model CartModel
@using SportsStore.Models
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

<style>
    body {
        background-color: #f4f6f9;
        font-family: 'Segoe UI', sans-serif;
    }

    .cart-title {
        font-size: 30px;
        font-weight: 700;
        color: #0f2e22;
        margin: 30px 0 20px;
    }

    .product-img {
        width: 90px;
        height: 90px;
        object-fit: cover;
        border-radius: 6px;
        border: 1px solid #ccc;
    }

    .product-name {
        font-weight: 600;
        font-size: 16px;
        margin-bottom: 4px;
    }

    .cart-item {
        border: 1px solid #ddd;
        border-radius: 10px;
        padding: 12px;
        margin-bottom: 16px;
        background: #fff;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.03);
    }

    .qty-box {
        width: 60px;
        text-align: center;
        font-weight: bold;
        border-radius: 6px;
        border: 1px solid #ccc;
    }

    .cart-summary {
        background: #fff;
        border: 1px solid #ccc;
        border-radius: 10px;
        padding: 20px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
    }

    .btn-action {
        font-size: 16px;
        padding: 12px 24px;
        border-radius: 6px;
        font-weight: 600;
    }

    .btn-update {
        background-color: #212529;
        color: #fff;
    }

    .btn-checkout {
        background-color: #1e8141;
        color: #fff;
    }

    .btn-checkout:hover {
        background-color: #166b36;
    }

    footer {
        margin-top: 80px;
        background: #0f2e22;
        color: #fff;
        padding: 40px 0;
    }

    footer .footer-col {
        margin-bottom: 20px;
    }

    footer a {
        color: #ccc;
        text-decoration: none;
    }

    footer a:hover {
        color: #fff;
    }
</style>

<div class="container">
    <h2 class="cart-title text-center">🛒 @Localizer["CartTitle"]</h2>
    <div class="row">
        <div class="col-lg-8">
            @foreach (var line in Model?.Cart?.Lines ?? Enumerable.Empty<CartLine>())
            {
                <div class="cart-item d-flex justify-content-between align-items-center flex-wrap">
                    <div class="d-flex align-items-center flex-grow-1 gap-3">
                        <img src="/images/@(string.IsNullOrEmpty(line.Product.Image) ? "default.jpg" : line.Product.Image)"
                             class="product-img" alt="@line.Product.Name" />
                        <div>
                            <div class="product-name">@line.Product.Name</div>
                            <div class="small text-muted">@(line.IsRental ? $"Thuê {line.RentalDays} ngày" : "Mua")</div>
                            <form method="post" asp-page-handler="Remove" class="mt-1">
                                <input type="hidden" name="ProductID" value="@line.Product.ProductID" />
                                <input type="hidden" name="isRental" value="@line.IsRental" />
                                <input type="hidden" name="returnUrl" value="@Model?.ReturnUrl" />
                                <button type="submit" class="btn btn-link p-0 text-danger small">🗑 Xóa</button>
                            </form>
                        </div>
                    </div>

                    <div class="text-end">
                        <div class="fw-bold text-danger mb-2">@line.LineTotal.ToString("N0") đ</div>
                        <input type="number" class="form-control qty-box mx-auto" value="@line.Quantity" readonly />
                    </div>
                </div>
            }
        </div>

        <div class="col-lg-4">
            <div class="cart-summary">
                <div class="fw-bold mb-2">Tổng số: @Model?.Cart?.Lines.Count() </div>
                <div class="fw-bold text-danger fs-4 mb-2">@Model?.Cart?.ComputeTotalValue().ToString("N0") đ</div>
                <p class="small text-muted mb-3">(Chưa bao gồm phí vận chuyển)</p>

                <form method="post" asp-page-handler="ProceedToCheckout">
                    <input type="hidden" name="returnUrl" value="@Model?.ReturnUrl" />
                    <button type="submit" class="btn btn-checkout w-100 btn-action">
                        🏍 TIến HÀNH ĐẶT HÀNG
                    </button>
                </form>

                <a href="@Model?.ReturnUrl" class="btn btn-update w-100 btn-action mt-2">
                    🏍️ TIẾP TỤC MUA HÀNG
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Modal Thêm vào giỏ hàng -->
<div class="modal fade" id="addToCartModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content p-3">
            <div class="modal-body">
                <div class="row">
                    <div class="col-md-6 border-end">
                        <div class="text-success fw-bold mb-3">
                            <i class="fa fa-check-circle fa-lg"></i> 1 sản phẩm vừa được thêm vào giỏ hàng
                        </div>
                        <div class="d-flex gap-3 align-items-start">
                            <img id="addedProductImage" src="/images/default.jpg" alt="Sản phẩm"
                                 style="width: 100px; height: 140px; object-fit: cover;" />
                            <div>
                                <div id="addedProductName" class="fw-semibold"></div>
                                <div id="addedProductDetails" class="small text-muted"></div>
                                <div class="fw-bold text-danger mt-2" id="addedProductPrice"></div>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="fw-bold mb-2">Giỏ hàng (<span id="cartItemCount">0</span> sản phẩm)</div>
                        <div>Tạm tính:</div>
                        <div class="fw-bold text-danger fs-5 mb-2" id="cartTotalPrice">0 đ</div>
                        <div class="d-flex gap-2">
                            <a href="@Model?.ReturnUrl" class="btn btn-outline-success flex-fill">← Tiếp tục mua hàng</a>
                            <a href="/Cart" class="btn btn-success flex-fill">Thanh toán →</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Footer -->
<footer class="mt-5">
    <div class="container">
        <div class="row text-center text-md-start">
            <div class="col-md-4 footer-col">
                <h5 class="fw-bold mb-3">RESTUDY STORE</h5>
                <p class="small">Nền tảng mua bán giáo trình, tài liệu học tập và kết nối tri thức sinh viên Việt Nam.</p>
            </div>
            <div class="col-md-4 footer-col">
                <h6 class="fw-semibold mb-2">Liên hệ</h6>
                <p class="small mb-1"><i class="fas fa-envelope me-2"></i>support@restudy.vn</p>
                <p class="small mb-1"><i class="fas fa-phone me-2"></i>0354 902 144</p>
            </div>
            <div class="col-md-4 footer-col">
                <h6 class="fw-semibold mb-2">Theo dõi chúng tôi</h6>
                <div class="d-flex gap-3 justify-content-center justify-content-md-start">
                    <a href="#"><i class="fab fa-facebook fa-lg"></i></a>
                    <a href="#"><i class="fab fa-twitter fa-lg"></i></a>
                    <a href="#"><i class="fab fa-instagram fa-lg"></i></a>
                </div>
            </div>
        </div>
    </div>
</footer>

<script>
    function showAddToCartModal(product) {
        document.getElementById("addedProductImage").src = "/images/" + (product.image || "default.jpg");
        document.getElementById("addedProductName").textContent = product.name;
        document.getElementById("addedProductDetails").textContent = product.details;
        document.getElementById("addedProductPrice").textContent = product.price + " đ";
        document.getElementById("cartItemCount").textContent = product.cartCount;
        document.getElementById("cartTotalPrice").textContent = product.cartTotal + " đ";
        var modal = new bootstrap.Modal(document.getElementById('addToCartModal'));
        modal.show();
    }

    document.addEventListener("DOMContentLoaded", function () {
        const params = new URLSearchParams(window.location.search);
        if (params.get("added") === "true") {
            showAddToCartModal({
                image: "@Model?.Cart?.Lines.LastOrDefault()?.Product.Image",
                name: "@Model?.Cart?.Lines.LastOrDefault()?.Product.Name",
                details: "@(Model?.Cart?.Lines.LastOrDefault()?.IsRental == true ? "Thuê " + Model?.Cart?.Lines.LastOrDefault()?.RentalDays + " ngày" : "Mua")",
                price: "@Model?.Cart?.Lines.LastOrDefault()?.LineTotal.ToString("N0")",
                cartCount: "@Model?.Cart?.Lines.Count()",
                cartTotal: "@Model?.Cart?.ComputeTotalValue().ToString("N0")"
            });
        }
    });
</script>